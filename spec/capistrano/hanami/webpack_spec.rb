RSpec.describe Capistrano::Hanami::Webpack do
  it "has a version number" do
    expect(Capistrano::Hanami::Webpack::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
